_target_: data.datamodule.CatDogImageDataModule

# Data directory
data_dir: "data"

# DataLoader parameters
batch_size: 8
num_workers: 0
pin_memory: false

# Dataset split ratios (must sum to 1.0)
train_val_test_split:
  - 0.8  # train
  - 0.1  # validation
  - 0.1  # test
